services:
  app:
    build: .
    ports: ["5000:5000"]
    env_file: .env
    volumes:
      - uploads:/app/uploads
      - instance:/app/instance
    restart: unless-stopped
volumes:
  uploads:
  instance:
